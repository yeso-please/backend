# 이 파일은 예시입니다. 실제 값은 config/application-secret.yaml(gitignore됨)에 넣으세요.
jwt:
  secret: "로컬-개발용-충분히-긴-무작위-문자열-여기에-생성해서-넣기"

# dev-rds profile에서만 아래 항목의 주석을 풀고 실제 값을 넣습니다.
# local profile을 사용할 때는 주석 상태를 유지해야 로컬 DB 설정을 덮어쓰지 않습니다.
# spring:
#   datasource:
#     url: "jdbc:postgresql://RDS_ENDPOINT:5432/tripin_dev?currentSchema=app&sslmode=verify-full&sslrootcert=ABSOLUTE_CA_PATH"
#     username: "tripin_app"
#     password: "APP_PASSWORD"
#   flyway:
#     user: "tripin_migrator"
#     password: "MIGRATOR_PASSWORD"
